name: Keep Page Active

on:
  schedule:
    # Cada 10 minutos de 8 AM a 10 PM (hora México UTC-6)
    # En UTC: 14:00-23:59 (lunes-viernes) y 00:00-04:00 (martes-sábado)
    - cron: '*/10 14-23 * * 1-5'
    - cron: '*/10 0-4 * * 2-6'
  workflow_dispatch:

jobs:
  visit-page:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3
      
    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
        
    - name: 🌐 Visit page
      run: |
        python keep_active.py
